@using DentistBookingForm.ViewModels
@model List<ApplicationUserViewModel>

@{
	ViewData["Title"] = "User Page";
}
<div class="container p-3">
	<div class="row pt-4">
		<div class="col-6">
			<h2 class="text-success">Users</h2>
        </div>
		<div class="col-6 text-end">
			 </div>
			 <a asp-controller="ApplicationUser" asp-action="Create" class="btn btn-success">
				<i class="bi bi-plus-circle"></i> &nbsp;Create New User
			</a>
</div>
<br/> <br/>


<table class="table table-bordered table-striped" style="width:100%">
	<thead>
		<tr>
			
			<th>Nr. de ordine</th>
			<th>User</th>
			<th>Email</th>
			<th>Number</th>
		</tr>
			@foreach(var user in Model)
			{
				<tr>
					<td>@user.Id</td>
					<td>@user.UserName</td>
					<td>@user.Email</td>
					<td>@user.PhoneNumber</td>
				</tr>
			}
		</thead> 
	<tbody>
		